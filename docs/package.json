{
  "name": "docus-starter",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "cd .. && yarn install && NODE_OPTIONS=--openssl-legacy-provider && build-storybook -o ./docs/.output/public/storybook && cd docs && nuxi build",
    "generate": "nuxi generate",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "nuxt": "^3.0.0",
    "@nuxt-themes/docus": "^1.4.7",
    "@storybook/addon-docs": "^6.5.16",
    "@storybook/addon-essentials": "^6.3.12",
    "@storybook/vue3": "^6.3.12"
  }
}
