{
  "name": "docus-starter",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "cd .. && yarn install && cd docs && nuxi build && cd .. && build-storybook -o ./docs/.output/public/storybook",
    "build:dev": "cd .. && yarn install && yarn prepare:playground && cd docs && nuxi build && build-storybook -o ./docs/.output/public/storybook",
    "generate": "nuxi generate",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "nuxt": "^3.0.0",
    "@nuxt-themes/docus": "^1.4.7"
  }
}
