{
  "name": "docus-starter",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "cd .. && yarn install && yarn storybook:build && cd docs && nuxi build",
    "storybook:build": "build-storybook -o ./docs/.output/public/storybook",
    "generate": "nuxi generate",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "nuxt": "^3.0.0",
    "@nuxt-themes/docus": "^1.4.7",
    "@storybook/addon-docs": "^6.5.16",
    "@storybook/addon-essentials": "^6.3.12",
    "@storybook/vue3": "^6.3.12"
  }
}
