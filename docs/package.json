{
  "name": "docus-starter",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build && cd .. && yarn install && yarn prepare:playground && build-storybook -o ./docs/.output/public/storybook",
    "generate": "nuxi generate",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "@nuxt-themes/docus": "^1.4.7",
    "@storybook/cli": "^6.5.16",
    "nuxt": "^3.0.0"
  }
}
